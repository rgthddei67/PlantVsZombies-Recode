---
name: project_pvz_gilded_zamboni
description: 2026-07-26 鎏金冰车僵尸、黄色冰道、活跃来源叠层、特殊植物交互与冒险 3-6 接入
metadata:
  node_type: memory
  type: project
---

# 鎏金冰车僵尸与黄色冰道

## 当前实现

`ZOMBIE_GILDED_ZAMBONI` 使用金黄色车体、驾驶员衣物和帽子，2200 HP，基础驱动倍率 0.72，
比普通冰车慢。正式波次每波最多生成 1 只，计数随 Board 存档并只在推进新波时清零；
AutoTest 的显式直造不消耗额度。冒险 3-6（level 24）为 20 波
`{normal, cone, bucket, zamboni, gilded zamboni}`。

车辆在本行与相邻两行碾压植物，包含相邻水路；黄色冰道只铺在非水路并保持 35 秒。
Board 分行保存黄色左缘/寿命，旧档缺字段为空；黄色与普通冰道重叠时先截短普通段再绘制
黄色段，因此玩家只看到黄色，且两者都禁止种植。

## 速度与受击

每个仍存活且实际覆盖目标的鎏金冰车都是一个独立速度来源。普通僵尸必须处在该来源已经铺过
的黄色区间；鎏金冰车本体另含短距离车身覆盖，使相邻车辆可互相影响。每层对能力、寒冰、
雨势、台风等非中性倍率分别应用“加速乘二、减速除二”，再把各分量相乘；中性 `1.0`
保持不变。两条活跃来源重叠会逐层放大，例如大雨 `1.4→2.8→5.6`、寒冰
`0.6→0.3→0.15`。来源死亡、车辆离开或范围变化后逐帧刷新层数。

自身连续 6/10/14 秒未受伤时基础蓄势为 ×2/×4/×8；任何本体伤害立即清零并回到 ×1，
自身黄色冰道也会强化这项能力，因此单来源下实际能力层为 ×4/×8/×8；最终能力倍率仍
封顶 ×8。计时器和每个来源的独立黄色左缘入僵尸扩展存档。

地刺只造成 100 本体伤害，不爆胎、不伤地刺；大嘴花每口只造成 50 本体伤害，不能吞下车辆。
两者都通过车辆虚事件接入，不在植物侧硬编码鎏金类型。寒冰仍可减速，冻结最终优先停格。

## 验证

`smoke_gilded_zamboni.json` 覆盖 2200 HP、基础速度、三行铺路/碾压、黄色覆盖普通冰道、
单来源天气/寒冰放大、双车互相两层、6/10/14 秒蓄势与受伤归零、水路只压不铺、地刺 100、
大嘴花 50、同波上限和跨波重置。旧版幅度放大方案曾可见运行通过；2026-07-26 主人试玩后
改为直接倍率方案，脚本期望同步为单层大雨 280、单层寒冰晴天 30、双层大雨 560，
无伤阶段 400/800/800。主人随后允许 AutoTest；更新脚本在当前桌面可见运行，窗口标题为
“植物大战僵尸中文版”、退出码 0，`run.log` 以 `script finished OK` 结束。状态与断言确认
单层大雨 280、寒冰晴天黄冰内/外 30/60、双层大雨 560、无伤 400/800/800 且受伤回 100；
双车叠层和受伤重置截图可见三路黄冰、鎏金车辆及对照目标。`clang-playtest` 与启用 LTO 的
`clang-release` 均零警告构建通过。

2026-07-26 补齐 `resources/info.txt` 的 `ZOMBIE_GILDED_ZAMBONI` 名称与描述；正文覆盖
三行碾压、黄色冰道、来源叠层/双车互助、无伤提速与特殊植物抗性，并保留宝开式自述。
主人明确本次不跑 AutoTest，图鉴排版由其自行验收。

同日主人试玩发现死亡入口仍固定触发普通 `ZamboniExplosion`，导致爆炸上方飞出的车体部件
保持蓝色。现在 `ZamboniZombie` 通过虚拟粒子名选择品种效果，鎏金变体使用独立
`GildedZamboniExplosion`：车盖、车轮和滚刷全部引用鎏金 reanim 贴图，两层爆炸云也改为
金黄色；普通冰车继续保持原蓝色效果。本次仍按主人要求不运行 AutoTest，只做构建与
XML/资源键静态校验，最终视觉由主人复验。

## 2026-07-27 黄色冰道来源专用索引

`Zombie::ComputeGoldenIceEffectStacks()` 原先由每只僵尸逐帧查询本行及相邻行：
首个 `ForEachZombieInRow()` 会在 `CleanupExpired()` 标脏后扫描全体僵尸重建行桶，
随后还要遍历三行候选并逐个 `dynamic_cast`。即使场上没有鎏金冰车，这项能力也会把
通用行索引拉进每帧热路径。

现在 `EntityManager` 在 `AddZombie()` 与 `AddZombieWithID()` 统一登记鎏金冰车的
ID→弱引用专用索引，每帧首查只从这张稀疏表生成活跃来源强引用快照；
`CleanupExpired()` 每帧释放快照，并随原有 30 帧清理周期裁掉过期弱引用。
目标仍即时复核来源当前行、活跃/死亡状态及 `ProvidesGoldenIceEffectAt()` 的精确覆盖，
零活跃来源时的 Board 持久黄冰回退也保持不变，因此进入、离开、来源死亡、范围变化和
多来源叠层语义未改变。复杂度从每目标扫描三行僵尸降为每目标只扫鎏金来源数，
并且该能力不再触发通用行索引重建；通用行索引的每帧标脏契约本身未修改。

`clang-playtest` 零警告构建通过。可见 `smoke_gilded_zamboni.json` 以 Seed 42 退出码 0；
新增用例确认跨相邻行两来源为 2 层、逐个击杀后依次变为 1 层，最后活跃来源清零时仍由
35 秒持久黄冰提供 1 层，`run.log` 以 `script finished OK` 结束。

详细设计见 `docs/superpowers/specs/2026-07-26-gilded-zamboni-design.md`。
