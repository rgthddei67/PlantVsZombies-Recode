---
name: project_pvz_winter_area_content_plan
description: 第七大关五种组合型僵尸、五株寒潮植物、获取顺序及已实现寒潮内容契约
metadata:
  node_type: memory
  type: project
  updated_at: 2026-08-25
---

# 第七大关组合威胁与寒潮植物

## 难度方向

主人要求第七大关回应“来点难的”，但难度来自组合、时序、路线和注意力争夺，不靠统一增加生命或攻击。僵尸规模控制为五种重点类型：气象干扰僵尸让本轮已公开寒潮预报失效且清除预报准备；冰裂钻机在冻土蓄力后沿本行释放持续地裂；冰墙工程师建立可持续推进、吸收直线火力并迫使自动投手优先拆墙的冰墙；冰像处刑者锁定冻土区高价值植物并以三次锤击完成处决；雪橇突击队在冻土冲撞后把成员向同行后方和邻行散开。它们应以“干扰预报→让预埋失误”“冰墙牵制投手并掩护蓄力”“雪橇打乱阵线→处决高价值目标”等编组出现。

## 植物顺序

奖励顺序固定为 7-1 雪锚果、7-2 融雪投手、7-4 伏霜雷、7-6 警铃草、7-7 炉芯花；7-3、7-5、7-8 和 7-9 无植物奖励。对应的首次可用关为 7-2、7-3、7-5、7-7、7-8。7-6 首次让气象干扰僵尸在玩家没有警铃草时完整生效一次；7-7 首次让玩家只凭警铃草处理冰像处刑者，通关后才获得炉芯花。

完整设计规格见 `docs/superpowers/specs/2026-08-24-winter-area-plants-design.md`。尚未实现的两株植物和两种僵尸不得用临时枚举或伪出怪占位；只允许由已实现内容提供目标拥有的窄接口，并在接口旁留下可搜索 TODO。雪橇车队、融雪投手、冰墙工程师、冰裂钻机与伏霜雷已按下方真实契约接入。

## 雪锚果首版契约

`PLANT_SNOWANCHORNUT` 是 7-1 通关奖励，初始调参为 100 阳光、30 秒冷却、3000 生命。它只能在未冻结格提前种植，所在格按 Board 当前真实温度成为冻土时自动进入锚定姿态；融化时退出。每个实体一生只有一次锚定承压，`braceSpent` 入植物快照，冻土资格本身不重复保存。

植物基类提供通用冬季冲击请求/响应：普通植物返回未拦截；雪锚果仅在当前格冻结、活动且未消费时原子消费次数。雪橇碰撞获得“拦截且禁止散射成员”的响应，地裂获得“拦截且后续伤害乘 0.5”的响应。威胁方仍拥有伤害值、动作提交时机、音画和下车/传播位置；未来实现不得在僵尸侧维护雪锚果类型表。雪锚果不影响冰墙、预报干扰或处决，也不获得通用伤害减免。

## 2026-08-24 实现与验证

雪锚果已作为独立 `SnowAnchorNut` / `PLANT_SNOWANCHORNUT` 接入，使用独立卡图、reanim 和普通/锚定各三档生命贴图；可复现资源由 `scripts/generate_snow_anchor_nut_assets.ps1` 生成。7-1 奖励已写入显式奖励表。实现没有预置五种僵尸枚举，正式敌人接入时消费 `WinterGroundImpactResponse` 即可。

`smoke_snow_anchor_nut.json` 共 82 条命令，覆盖资源注册与键加载、100/30/3000 调参、普通植物中性响应、冻土资格、冻结格拒种、雪橇/地裂两种原子响应、重复请求、三档受损材质、解冻/再冻结、快照往返与 7-1 奖杯奖励。`clang-release` 全量 LTO 构建零警告，主程序 Win7 导入审计通过 378 项，`SaveSchemaTests` 通过；专项在当前桌面可见的默认 Vulkan 实例路径和 `-NoInstance` 路径均执行至 command 81、exit 0、`status=passed`、`script finished OK`，两路径截图均目验锚爪、裂纹和解冻回切。既有 `smoke_winter_garden.json` 同次 Release 默认 Vulkan 可见回归执行 144 条命令至 command 143 并通过，暖态、霜线、暴雪和读档截图正常。

## 2026-08-24 雪橇车队实现

`ZOMBIE_BOBSLED_TEAM` 已接入 7-2、7-3、7-5、7-7、7-8、7-9。一次正式候选生成队长与三名跟随者，雪锚果的 `WinterGroundImpactResponse` 现已由真实车辆碰撞消费：普通路径跨行散开，锚定路径四人保持在植物右侧同一行。完整行为、资源、存档和验证证据见 `project_pvz_bobsled_team_zombie.md`。

## 2026-08-24 融雪投手实现

`PLANT_MELTSNOWPULT` 是 7-2 通关奖励，当前调参为 125 阳光、7.5 秒冷却、300 生命、约 2.86～3.0 秒一次投掷。普通融雪团和盐晶均造成 20 点本体伤害；一次准确寒潮预报的出现沿把每株库存直接补至上限 3，新种下的融雪投手若正处于有效预报也从 3 发开始。气象干扰通过 Board 唯一入口让本轮预报失效，并清空所有已落地植物的库存与尚未出手盐晶；已经离手的盐晶仍独立飞行和入档。

盐晶额外携带 200 点独立冰制层腐蚀值，不得把未用完的腐蚀折算为本体伤害。同行存在冰墙时融雪团和盐晶都优先瞄准墙体：普通团只结算 20 直击，盐晶结算 20 直击＋200 腐蚀；未来冰制护甲僵尸仍通过 `Zombie::ApplyWinterCorrosion` 的中性虚接口接入。Board 的 `coldWaveForecastDisrupted` 与融雪投手库存、蓄力状态、空中盐晶均进入正式关卡快照。

独立卡图、reanim、盐晶和两套命中粒子由 `scripts/generate_melt_snow_pult_assets.ps1` 可复现生成。`smoke_melt_snow_pult.json` 共 92 条命令，覆盖资源键、125/7.5/300/20 调参、预报补满、种植初始库存、干扰清库存与蓄力、空中盐晶跨干扰及读档存活、普通目标只承受 20 本体伤害、200 腐蚀投影、粒子、库存快照与 7-2 奖励。`clang-release` 全量 LTO 构建及 `SaveSchemaTests` 通过；专项在当前桌面可见的默认 Vulkan 实例路径和 `-NoInstance` 路径均执行至 command 91、exit 0、`status=passed`、`script finished OK`，同步截图目验全株冷色资源、空中盐晶和蓝白命中提示。同次 `smoke_winter_garden.json` 至 command 143、`smoke_cabbagepult.json` 至 command 172，均在 Release 默认 Vulkan 可见回归通过。

## 2026-08-24 冰墙工程师实现

`ZOMBIE_ICE_WALL_ENGINEER` 为 800 本体＋370 安全帽，复用路障死亡/啃食时间轴而不新增帧事件。完全进入草坪且已有冻土后立即部署 600 生命未完成墙并停步施工 4 秒；墙立刻挡平射，施工受冻结、黄油与麻痹自然暂停，施工墙被拆、断头、死亡和魅惑会使半墙碎裂并消费能力。完工在同一对象上硬化至 1800 生命，之后工程师死亡不拆墙。

冰墙每游戏秒向房屋推进 14px，在同行最近植物 collider 右侧保留间隙停住；`THAWING` 或高于冻结温度时每秒融化 120。同行平射弹在僵尸碰撞前命中墙体；卷心菜、玉米/黄油、西瓜/冰瓜及融雪团/盐晶自动投手在同排有墙时优先锁墙，伤害与状态不穿透到墙后或邻行僵尸。普通手动/测试抛射仍可越墙，玉米炮不受影响；火焰伤害两倍，盐晶额外结算 200 独立腐蚀。工程师每正式波最多一只，接入 7-3、7-4、7-5、7-8、7-9，7-6/7-7 留给其他机制教学。

安全帽、橙黄反光背带、工具带和双箍制冰罐随既有身体/领带/路障轨道运动；墙体三档贴图与四套粒子由 `scripts/generate_ice_wall_engineer_assets.ps1` 确定性生成并锁定 SHA-256。施工阶段、计时、目标墙位、能力消费、墙体位置/生命/融化余量/完成态/施工者 ID 及每波计数均进入正式快照。`clang-release` 全量 LTO、378 项 Win7 导入、`SaveSchemaTests`、`SaveMigrationTests` 通过；工程师专项扩至 129 条命令并在默认 Vulkan 与 `-NoInstance` 可见路径通过，出怪表、融雪投手及冬日花园父回归同次 Release 通过。

## 2026-08-25 冰裂钻机实现

`ZOMBIE_ICE_CRACK_DRILL` 为 650 本体＋900 可盐蚀钻机层，完全进场且脚下冻土时停步蓄力 3.5 游戏秒，随后提交独立同行地裂。地裂以 180px/游戏秒向房屋传播并逐格造成 500 伤害；雪锚果自身承受完整一击后把左侧后续伤害减至 250。冻土融化和通用中断允许重试，死亡、断头、断臂、魅惑或碎机永久消费；状态、每波上限和独立地裂均进入快照。

独立钻机按源图 50% 绘制，稳定挂在身体轨并让右手掌盖住握把，充能只做约 ±1px/±0.5px 的微颤；三档安全帽和钻机破损、四相钻齿、地裂与掉落粒子均为独立资源。7-4 首发，7-5/7-8/7-9 复习，7-6/7-7 暂退。完整合同与 Release 双路径证据见 `project_pvz_ice_crack_drill_zombie.md`。

## 2026-08-25 气象干扰僵尸实现

`ZOMBIE_WEATHER_JAMMER` 复用 270 本体＋1100 可磁吸铁桶，设备非磁性。完全进场且累计余时未到上限时
立即抢占啃食并原地 idle 施法 4 游戏秒，不再等待公开预报；每次提交向整栏黑障余时追加 30 游戏秒，
同时干扰仍公开的雨雪/待生效台风警报、寒潮和雾势，窗口内后来广播也当帧截获，锁定实况与正式切档
不变。多只同类可在活动窗口内继续起手，当前同时余时上限 300 秒；通用外部中断后重启 5 秒并允许
走路/啃食，死亡、断头、断臂和魅惑永久消费。冻结/黄油/麻痹暂停施法，减速按 0.5 倍推进。

权重 2600、第五波起、生存第 13 轮起、每波最多一只；7-6 第三波额外保底且消费唯一名额，7-7
暂退，7-8/7-9 组合复习。手绘背包与手持终端使用独立子 Animator，最终背包与肩背/腰线重叠，
默认实例与 `-NoInstance` 截图已经主人确认。完整 Board 可见性、存档、资源崩溃修复和验证证据见
`project_pvz_weather_jammer_zombie.md`。

## 2026-08-25 伏霜雷实现

`PLANT_FROSTMINE` 是 7-4 通关奖励，初始调参为 50 阳光、30 秒冷却、300 生命。准确寒潮预报预测其所在格将冻结时从 `DORMANT` 进入 `CALIBRATED`；真实冻土到达后提交为 `ARMED`。预报干扰只把尚未兑现的校准退回休眠，已经武装的埋伏不随干扰、回暖或下一轮预报撤销，并在存档往返后保持。武装态不可啃食，只选择碰撞范围内活动、未魅惑且可受地面机关影响的目标；同帧有多个目标时按稳定僵尸 ID 取最小值，飞行目标不会踩雷。

爆裂先打断目标尚未提交的特殊动作，再结算 1000 点目标自有冰制层腐蚀和 600 点植物来源常规伤害，腐蚀不得溢出本体。该顺序可完整破坏冰裂钻机 900 点钻机层，把未提交蓄力退回移动且保留重试资格，不产生地裂；普通杂兵会被直接炸倒。三阶段均使用独立完整立绘而非换色，武装态明显埋入冰雪；卡图、碎冰与霜环粒子均有独立资源，可由 `scripts/generate_frost_mine_assets.ps1` 从锁定概念源复现。完整合同与最终 Release 证据见 `project_pvz_frost_mine.md`。
