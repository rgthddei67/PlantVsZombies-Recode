# 黑夜随机雨势天气

> 2026-08-15 范围更新：通用雨势由 `Board::SupportsWeather()` 统一判定；冒险模式从 2-1（level 10）起均启用，一大关关闭；白天、黑夜和泳池生存 level 1000/1001/1002 均启用。独立迷雾默认仍由 `Background::NIGHT_WATER_POOL` 提供；明确复用完整迷雾的固定冒险关集中登记在 `AdventureProgression::HasLevelSpecificFogMechanics()`，当前仅 6-9 的 `NIGHT_ROOF`。下文大量“黑夜/四大关”描述是历史建设语境，当前以此段和源码为准。

## 当前实现（2026-07-21）

### 独立迷雾（2026-08-15）

`NIGHT_WATER_POOL` 背景现有独立于雨势的 `FogWeatherIntensity::DEFAULT/SMALL/NORMAL/DENSE` 四档雾势状态机，由 `Board` 唯一持有；6-9 的 `NIGHT_ROOF` 通过上述关卡函数复用同一状态机、雾势预报与存档。基础雾线仍按九关制的关内进度换算为 4-1 基准列 6、4-2～4-6 基准列 5、4-7～4-9 与 6-9 基准列 4。主人当前定案为：默认雾势保持原版长度并只画原版主层，小雾向屋侧扩 1 格、普通迷雾扩 2 格且两者都加 1 个错帧补层，大雾扩 3 格并加 2 个错帧补层。每层仍使用原生 210×190 雾片按当前 Board 行高重叠，逐格稳定二维伪随机选择 `fog.png` 的 8 帧，不铺固定白底幕。`fog.png` 由 `fog.jpg` RGB 与 `fog_.jpg` 灰度 alpha 合成，资源清单按 `Column=8/Row=1` 切分。

雾势独立计时：战斗开始 45～70 秒后首次抽取；随后按“原版默认雾休整 → 小雾/普通迷雾/大雾事件 → 默认雾休整”循环。默认段持续 50～80 秒，增强雾势事件持续 35～55 秒；事件中大雾概率随既有天气导演从主人当前的 45% 平滑到 85%，剩余概率在小雾和普通迷雾间尽量均分。阶段结束前 15 秒锁定准确雾势预报，和雨势预报同时显示但不复用雨势误报语义。

只有超强台风会增加同一 `fogDispersal`，每游戏秒驱散 0.28，完全驱散会提前结束当前增强雾势事件并回到默认雾休整；普通和强台风不再削弱迷雾，已有驱散量还会按每秒 0.06 回流。所有台风仍保留风向视觉反馈：吹向屋子时所有雾层向负 X 漂移，吹向前线时向正 X 漂移，最大 180px；层数不建立独立存档状态，全部消费同一逐格 alpha、驱散和偏移，因而不会留下补层残影。雾势、预报、阶段计时、驱散与偏移进入关卡存档；关卡 schema v3 把 v2 的旧 `CLEAR/DENSE` 值迁移为视觉等价的 `SMALL/DENSE`，v1/无版本旧档仍由当前关卡上下文保留 `StartGame()` 建立的原版默认雾。

`smoke_fog_weather.json` 164 条命令可见 exit 0，覆盖非资格背景 no-op、4-1/4-6/4-7 原版默认雾线、默认/小雾/普通迷雾/大雾的 0/1/2/3 格扩展与 1/2/2/3 层、普通/强台风只漂移但不驱散、超强台风完全吹散、停风回流、雾势预报与隔离快照往返。`smoke_level_6_9_fog.json` 98 条命令可见 exit 0，锁定 6-8 无雾、6-9 默认雾/动态大雾、路灯花燃料与逐格照明、雾状态快照往返，并直接证明关灯时远处雾中目标不可索敌、III 档照亮后恢复、关灯进入大雾后再次阻断；四张截图已目验。视觉截图必须同时确认默认雾保持原版稀密度、增强雾势补层无固定白底幕、台风吹净后无残影。

### 雨势天气

黑夜关用间歇式小/中/大雨替代墓碑机制；白天背景不启用。战斗开始后 80～90 秒出现首场雨，完全放晴后仍保留 15～40 秒恢复间隔。天气导演强度取既有场次成长与独立压力曲线的较大值：新天气“续晴/小雨/中雨/大雨”权重从前期 15/40/40/50 平滑到满压力 0/10/25/65；导演达到 75% 后，若本轮新天气不是大雨，下轮新天气强制大雨，故后期最多只间隔一轮弱天气。该连续弱天气计数进入关卡存档，旧档默认 0。前期新雨仍统一持续 85～150 秒；满压力新小雨缩短到 45～70 秒、中雨 60～95 秒，大雨保持 85～150 秒。尾雨前期统一 45～80 秒，满压力小雨缩短到 20～35 秒、中雨 30～50 秒，大雨保持 45～80 秒。初始小雨结束时中雨/大雨/放晴权重为 30/45/30；中雨的“小雨/晴天”由 20/70 变到 15/95，大雨的“中雨/小雨/晴天”由 45/20/40 变到 55/5/40；新中/大雨各至多拥有一次权重 15/20 的同档续期。续期或切档后只允许衰减，尾端小雨必定放晴，雨链仍然有界。天气强度、阶段剩余时间、大雨闪电计时、小雨增强资格、同档续期资格、过渡来源雨势与过渡余时随关卡存档；瞬态粒子和地面水花不存档，`StartGame()` 按剩余天气重建。

天气预警：每个晴/雨阶段结束前 15 秒预抽取并锁定真实下一天气；公开预报准确率随天气导演从前期 75% 平滑提高，最高严格封顶 90%。误报只从当前动态非零权重、且不同于真实结果的候选中选择；满压力续晴权重为 0 时不会再预报续晴，弱天气保底要求大雨或无增强资格的尾端小雨只有单一合法候选时强制报准。预报与当前天气相同时，牌子明确追加“（持续）”。左上角面板用未缩放时间滑入/滑出，第一行显示当前天气，第二行显示公开预报与揭晓倒计时；公开预报为大雨且已锁定等级不是 `NONE` 时追加“台风/强台风/超强台风”。该后缀只消费公开大雨的锁定警报等级：误报大雨显示同分布诱饵，公开非大雨则隐藏真实待生效台风，不能成为新的真假旁路。主底色 alpha 为 176，错位阴影 alpha 为 64，文字、边框与左侧强度色条仍保持原透明度，避免展板遮住战场。天气揭晓后继续显示当前结果 5 秒，随后即使仍在下雨也会收回，直到下一次预警再出现。公开雨势或公开台风等级任一项与揭晓实际值不同，都会在面板下方弹出约 3.2 秒的非模态红色提示，完整显示“预报：雨势·台风等级 → 实际：雨势·台风等级”，不暂停也不抢输入；大雨同档续期不会消费新预抽的台风，实际等级必须与当前 `Board::mTyphoonStrength` 比较。公开预报、真实下一天气、ready 标记和当前天气展板的剩余显示秒数均进关卡存档；失败提示的剩余时间、错误/实际雨势和错误/实际台风等级也一起保存，读档从原动画进度继续，归零后不重播；旧档缺少新增等级字段时以 `NONE` 中性兼容，无需提升 schema。只有主面板的滑动进度不存档。

大雨分级警报：阶段结束前 5 个游戏秒，中央文字横幅只由公开天气预报驱动——公开预报为大雨就显示，否则即使真实下一天气为大雨也隐藏，使弹窗有无不再透露预报真假。公开误报大雨时仍用真实大雨完全相同的权重锁定警报等级和文案编号并随关卡存档恢复；揭晓为非大雨时清除，不改变当前台风、保底、天气或玩法倍率。真实下一天气为新大雨时仍同步预抽完整待生效台风初态；公开预报不是大雨时虽然隐藏文字，切档仍消费同一份结果。无台风、台风、强台风、超强台风各有 3 句同级文案并等概率锁定，每档前两句为“七字，七字”且同句押韵的唐诗式对句，第 3 句用全大写英文制造突发灾难警报的反差。文字不直报等级，只通过逐级增强的辞势与冷蓝、金黄、橙红、绯红色阶暗示威力。`GameScene` 的提示由单实例改为按出现顺序保存的图片/文字队列，既有“一大波僵尸正在接近”与大雨警报可以同时存活，越晚出现越晚绘制。待生效等级、风向、首档计时、阵风预算与文案编号均进关卡存档，读档不重 roll。

大雨附加台风：每次真正进入新大雨阶段只判定一次，基础概率随天气导演从前期 75% 平滑升到满压力 95%；每次落空后下次额外增加 20 个百分点并封顶 100%，因此满压力大雨最多落空一次。保底次数随关卡存档，读档不重 roll，也不能靠反复读档清掉落空记录。命中后普通/强/超强台风权重从前期 40/45/45 平滑到满压力 15/45/40，强/超强合计 85%，超强占 40% 但仍不过半，并保留 15% 不移动植物的普通台风作为缓冲。强度只按 `超强台风 → 强台风 → 台风 → 结束` 单向衰减，不在同一场大雨中重新增强；超强/强/普通台风衰减前分别维持 55～70、50～65、50～55 游戏秒。风向每 15～25 游戏秒独立二选一重抽，抽中当前方向则继续维持。台风期间天气面板常驻并显示实时风向；普通台风显示“持续风（无位移）”，强/超强台风显示阵风倒计时或阶段结束。强度衰减会重新显示 5 秒当前天气通知，普通台风消散后面板恢复原本的预报生命周期。

台风玩法：僵尸自主水平位移先应用大雨 1.40，再乘台风相对倍率；敌对僵尸顺/逆风在普通、强、超强三档依次为 1.08/0.92、1.14/0.86、1.20/0.80，魅惑僵尸因实际移动方向相反而交换顺逆风。台风不改 Animator、攻击或技能计时。普通台风没有阵风位移；强台风等待 12～16 秒、单次 1 格、活动 1.8 秒且每阶段最多 3 次，超强台风等待 14～17 秒、单次 2 格、活动 2.4 秒且每阶段最多 2 次；等待上限均短于对应最短衰减时间，避免自动流程尚未起风就先降档。活动阵风开始时锁定强度与风向，植物结算点一次性预抽在 25%～75% 进度内；衰减与转向计时在这段短阶段暂停，结束后才继续。阵风按风向从前缘逐格结算植物：目标有植物则留在原格，吹入弹坑或越过地图边缘则直接死亡。弹坑本身保留，不能作为挡风墙保护相邻或后方植物。

活动阵风同时对全部存活僵尸施加独立于自主行走的物理漂移：强/超强峰值为 18/32 像素每游戏秒，使用 `4p(1-p)` 包络从 0 平滑升到峰值再回落，单次总漂移约 22/51 像素，明显小于植物 1/2 格。冻结和啃食中的僵尸仍会连同 Transform/碰撞箱一起被吹；魅惑只改变自主行走方向，不改变空气的物理吹向。吹向前线时横坐标钳在画面右缘外 60 像素，低于既有 +65 像素清理线，防止刚出生僵尸被阵风误删；吹向房屋不设额外保护，保留天气威胁。阵风把啃食者吹离目标时由正常碰撞退出链停止啃食。

轻型子弹响应实时风向：当前豌豆、寒冰豌豆、孢子、火豌豆、星弹、僵尸豌豆、毒豆及毒火球按自身 `velocityX` 判断顺逆风；普通/强/超强三档水平速度倍率依次为 1.15/0.85、1.25/0.75、1.45/0.55，命中伤害倍率依次为 1.10/0.90、1.15/0.85、1.20/0.80。投掷物、尖刺、重型弹丸和篮球不响应。倍率只在 `Bullet::Update()` 与 `Bullet::BulletHitZombie()` 的公共结算点从当前 Board 台风状态派生，不改写基础速度/伤害，故风向翻转会立即影响在途子弹，放晴自动还原，存档仍只保存基础值。`Bullet.cpp` 的风力档案表必须与 `BulletType` 数量和顺序完全一致；新增或重排枚举而未明确选择风力响应会触发编译失败。

植物换格把逻辑 `row/column`、Cell 占用、Transform 与碰撞箱在随机结算当帧立即切到目标格，再以 0.45 游戏秒纯视觉偏移让本体、阴影和血条平滑追赶；视觉锚点从源/目标 `GetCellCenterPosition` 插值完整二维坐标，因此屋顶坡面不会只横移后浮空。存档只记录已经结算的目标格，不记录视觉偏移；因此滑动中保存/读档会直接落在目标格，不会卡在半格。台风强度、风向、强度衰减余时、转向余时、阵风等待、剩余预算，以及活动阵风的锁定强度/方向、总时长、余时、植物结算余时和已结算标记均入关卡存档；读档继续剩余僵尸漂移，且植物至多结算一次。僵尸实际漂移后的 Transform 本来就在僵尸存档中；风线粒子与植物视觉插值仍属瞬态，读档后按逻辑状态重建或归零。

倍率口径：基础僵尸小/中/大雨倍率为 1.15/1.25/1.40；独立天气压力在普通关从 40% 波次进度开始、到 75% 达到完整压力，黑夜无尽则从第 8 轮到第 20 轮单独 smoothstep，完整压力下三档平滑到 1.20/1.35/1.55。30 波普通关的第 20 波历史断言约为 86% 压力；2026-07-27 当前源码实测为 97%，后续须重新核对压力曲线。倍率统一进入 `Zombie::UpdateAnimSpeed()` 的 extra 层，组合式为 `GetAbilityAnimSpeedMultiplier() × slowFactor × rain`，冻结优先置 0；正式波次或轮次推进会刷新现存僵尸。植物小/中/大雨倍率由前期 1.10/1.15/1.20 随同一压力平滑到 1.00/0.97/0.93，只通过 `Plant` 辅助方法影响攻击、生产、成长和恢复，不改全局 delta，也不影响索敌、受伤、死亡、爆炸清理或卡片冷却。僵尸与植物倍率都由已持久化的波次/轮次派生，不新增存档字段；只有影响未来抽取的弱天气保底计数需要单独存档。

视觉：`RainLight/Medium/Heavy.xml` 位于两个 `build/<preset>/resources/particles/config/`，复用 WhitePixel，使用细、低透明、冷蓝、逐滴亮度不同的斜雨丝。小雨为初始 72 粒、每秒 80 粒、alpha 峰值 .25；中雨为 52/60/.25，但通过更粗、更长、更快及更暗的幕保持强度差异，大雨为双层发射器。台风期间大雨改用 `RainHeavyTowardHouse/Front.xml`：吹向屋后以 X=-430/-480、正 28°～37° 形成左斜雨，吹向前线以对称正 X 与负角度形成右斜雨；翻向时 `ParticleSystem::StopEffect` 只停止旧雨继续发射，在途雨丝自然淡出，新方向按剩余雨时立即接管，不能用 `ClearAll()` 误杀爆炸和水花。天气通过正的运行期时长把发射器切成循环模式，`SpawnMaxLaunched` 只作为可复用池容量，不再用累计总配额硬撑整场雨；`Board` 每帧核对当前雨势发射器，若被清理或异常停止会按剩余时间自动补发。地面水花沿用原版 `Rain_splash1～4` 与 `Rain_circle` 贴图，每次先在 9×5 网格内随机 X，再用该 X 的 `GetRowCenterYAtX` 建立对应行地面窄带，屋顶坡面不会浮空或穿瓦；按较大游戏窗口定稿为小/中/大雨平均约每秒 5/10/20+ 次（间隔 0.16～0.24、0.08～0.12、0.02～0.04 秒），落点距网格边缘保留 18px，避免素材跑出屏幕。`ParticleRotation` 显式使用时走“世界中心旋转后再拉伸”的矩阵路径，避免细长贴图角度被压回竖直。暗幕在默认世界粒子之后、UI 之前绘制，统一压暗背景、实体、雨丝、爆炸和命中飞溅，但不压卡片、按钮与文字；当前 alpha 小/中/大为 34/55/110。仅大雨触发程序化闪电：每次固定 10 段冷白主干与 3 组二段分叉，使用独立视觉随机序列避免扰动天气/出怪；0.42 游戏秒内播放主放电与较弱回闪，并在云层和草坪落点绘制有限半径蓝紫散射光。闪电在世界暗幕后、UI 前合成，不再调用寒冰菇的全屏白闪；路径、光照与视觉序列均属瞬态，不入存档。

风线视觉：两个 preset 都有 `WindTowardHouse.xml` 与 `WindTowardFront.xml`，共用原创 `WindStreak.png`，分别从画面右/左边缘外横穿逻辑画面。该 32×256 RGBA 纹理由内置 `imagegen` 在纯品红背景生成单根冷蓝风痕，再按技能流程去色键、验证透明角与半透明边缘后缩图入库；尖端、亮芯和柔和光晕让它在复杂草坪上可见而不再呈硬白直线。每批仍为 10 粒，`ParticleScale [.32 .50]` 与 `ParticleStretch [.75 1.10]` 形成约 61～141px 的随机长度，峰值 alpha .22。三档强度只由 `Board.cpp` 顶部发射间隔 0.30/0.20/0.10 秒控制，因而普通最疏、强台风居中、超强台风最密；风向翻转时立即发射新方向，旧方向粒子在最长 1.15 秒寿命内自然淡出。粒子对象不进存档。

声音：对照 C# 原版 `Challenge.InitLevel()` 的 `PlayFoley(FoleyType.Rain)`、`TodFoley` 中 `SOUND_RAIN` 的 flags=5（Loop + MuteOnPause）以及 `Board.DisposeBoard()` 的 `StopFoley(Rain)`。复用原版 `rain.ogg`，本实现通过 `AudioSystem::PlayLoopingSound/StopLoopingSound` 按资源键唯一追踪循环声道；小/中/大请求音量为 0.20/0.28/0.48，仍乘主音量与音效音量。切档时僵尸倍率、植物倍率、暗幕 alpha 与雨声音量共用 2 游戏秒 smoothstep 过渡，旧雨丝自然收尾并与新发射器短暂重叠；雨转晴时等淡出结束才停止循环声。SDL_mixer 的声道音量会跨播放保留，因此循环状态同时保存 `Mix_Chunk*`：停止、查询和调音前校验声道仍属于该资源，回收后恢复 `MIX_MAX_VOLUME`；普通音效播放也主动复位取得的声道，避免复用低音量雨声声道后随机“吞音效”。

## 资源与验证

- 后续新增雨天专属能力、精英僵尸雨势强化、雨天变异/条件生成或继续修改天气状态机时，先使用 `.agents/skills/adding-rain-weather/SKILL.md`；新僵尸、植物、粒子或生存词条同时叠加各自技能。
- 雨 XML、`WindStreak.png` 与 `rain.ogg` 都在忽略的 build 资源树，提交时必须 `git add -f` 两个 preset；`resources.xml` 两份需保持一致。
- `adding-particle` skill 已补 `ParticleRotation` 标签语义；再改 loader/emitter 消费端要同步技能。
- `smoke_night_rain.json` 用 `set_weather`/`advance_weather_phase`/`trigger_lightning` 固定状态，断言三档倍率、暗幕、雨声启停、词条组合、减速、冻结优先级、大雨程序化闪电及“小→大→中→小→晴”完整链，并产出七张截图；另用 0.2 秒真实天气倒计时验证尾端小雨自动复位倍率并停止声音，用第 28 秒截图验证长雨段仍持续发射粒子。`smoke_lightning_visual.json` 专门断言闪电不启用 `screenFlashOn`、主干/分叉结构与落点边界，并同步截取主放电和回闪。
- 2026-07-20 `clang-release` 零警告；可见 `smoke_night_rain` 76 命令通过；既有白天 `smoke_perks_attackspeed` 通过且天气倍率保持 1.0。
- 2026-07-20 针对第二轮实玩问题完成循环粒子池、自愈检查和音频声道复位修复，`clang-release` 零警告；按主人要求不跑 AutoTest，改由正常游戏实玩验收。
- 2026-07-20 主人提交 `5239d44` 调整天气时间、权重、倍率、暗幕和音量；随后新增原版贴图地面水花，`clang-release` 零警告，按主人此前要求继续用正常游戏实玩验收而不跑 AutoTest。
- 天气预警新增 `set_weather_forecast` 确定性入口；`dump_state.weather` 暴露公开预报、当前天气提示、面板滑入进度和失败提示状态。`smoke_weather_forecast.json` 覆盖 15 秒预警、错误揭晓、正确预报不误报、揭晓后 5 秒显示以及超时隐藏；`smoke_night_rain.json` 的平衡数值断言已同步主人当前参数。
- 2026-07-20 初版天气预警 `clang-release` 零警告；可见 `smoke_weather_forecast` 30 命令与更新后的 `smoke_night_rain` 76 命令均通过，退出码均为 0。预警面板和“预报中雨 → 实际大雨”失败提示截图已人工检查，中文、层级和位置正常。随后按主人反馈加入揭晓后 5 秒收回、展板计时及失败提示的存档恢复，三次 `clang-release` 增量验证均零警告；脚本已补 5 秒生命周期断言，主人要求不再主动启动，新增存档续播留待其自行实玩。
- 2026-07-20 后期权重、合法预报候选、同档“持续”与 2 秒过渡完成后，`clang-release` 零警告；当前桌面可见运行 `smoke_weather_forecast` 58 命令和 `smoke_night_rain` 81 命令均通过，退出码均为 0。人工检查“晴天（持续）”与错误揭晓截图，文字、层级、位置和切档中间亮度正常。
- `smoke_typhoon.json` 用 `set_typhoon`/`trigger_typhoon_gust` 固定三档强度、风向、阵风与衰减计时，覆盖普通台风不移动植物、强台风活动前半植物仍留原格、预定时刻 1 格结算、冻结僵尸持续漂移、超强台风 2 格、边缘消失、顺逆风相对雨天倍率、风线密度、单向衰减、阵风预算不补充和离开大雨清空状态；`dump_state.weather` 同时暴露活动阵风锁定状态/余时/植物结算标记/僵尸瞬时风速、强度衰减、转向、移动/损失计数以及敌对/魅惑组合倍率。
- `smoke_typhoon_bullets.json` 用 `spawn_bullet` 固定豌豆、寒冰豌豆和孢子的基础速度/伤害，覆盖三档顺逆风派生值、孢子豁免、放晴还原，并以超强逆风下 100 点豌豆把普通僵尸从 270 打到 182 验证真实碰撞伤害链。
- 2026-07-21 完整 `clang-release` 配置与构建退出码为 0；当前桌面可见运行 `smoke_typhoon` 77 命令、`smoke_night_rain` 81 命令和 `smoke_weather_forecast` 58 命令全部通过，退出码均为 0。人工检查普通/强/超强台风、强台风滑动中间帧及超强台风位移后截图，风线浓度递增、左右方向、加宽后的中文边界、本体/阴影/血条同步均正常。
- 2026-07-21 主人以 20 倍速实测 10 分钟仅遇到一次普通台风后，将基础概率由 8%～20% 提高为 25%～50%，加入每次落空 +15、最高 85% 的跨大雨保底，并把命中后的三档权重由 70/25/5 调为 50/35/15。完整 `clang-release` 配置与编译退出码为 0；`smoke_typhoon` 已补固定点数与保底状态断言，但按主人要求本轮不启动 AutoTest，由主人自行实玩验收。
- 2026-07-21 轻型植物子弹接入台风顺逆风速度与伤害后，完整 `clang-release` 配置/构建退出码为 0；当前桌面可见运行 `smoke_typhoon_bullets` 39 命令和更新后的 `smoke_typhoon` 86 命令均通过，窗口标题确认且退出码均为 0。前者的实际碰撞、放晴还原、状态 JSON 与两张截图均已检查，后者同时补验此前尚未运行的概率保底断言以及普通台风“持续风（无位移）”UI。
- 2026-07-21 阵风改为强 1.8 秒/超强 2.4 秒的可存档活动阶段，植物在 25%～75% 进度内预抽一次结算时刻，僵尸按强 18/超强 32 像素每秒峰值平滑漂移；强/超强等待同步调为 12～16/14～17 秒，消除衰减早于首阵风的矛盾。完整 `clang-release` 配置/构建退出码为 0；当前桌面可见 `smoke_typhoon` 108 命令和 `smoke_typhoon_bullets` 39 命令均通过、窗口标题确认、退出码 0，截图确认“阵风中”和普通台风“持续风（无阵风位移）”均未越界。
- 2026-07-21 按主人实玩反馈把原先仅约 4～13px、几乎看不见的风线放大后再收敛，最终采用约 25～67px 的低透明冷蓝短风痕；两个 preset 的左右风向 XML 完全一致。当前桌面可见临时视觉脚本依次固定普通/强/超强台风，15 条命令通过、窗口标题确认、退出码 0，三张截图人工检查后确认普通档不抢雨幕、强度随密度递增且不再呈硬白长条。既有 `smoke_typhoon` 在粒子检查前即因旧平衡断言仍期望 25%、当前源码实际 35% 而退出 1，与本次纯 XML 调参无关。
- 2026-07-21 复杂场景仍吞没低透明 WhitePixel 后，改用内置 `imagegen` 生成并去色键的原创 `WindStreak.png`，同时让台风大雨随实时风向切换左右斜率；定向停止旧雨的接口保留在途粒子自然收尾。完整 `clang-release` 配置/构建退出码 0；当前桌面可见 `smoke_typhoon` 117 条与 `smoke_night_rain` 84 条命令均通过、窗口标题确认、退出码 0，状态断言覆盖 `RainHeavy → RainHeavyTowardHouse → RainHeavyTowardFront → RainHeavy → RainMedium`，人工检查普通/强/超强截图确认风痕在复杂草坪可读、屋后左斜雨与前线右斜雨方向正确。两份旧平衡脚本断言同步校准到当前源码。
- 2026-07-21 按玩家实战反馈把 GameAPP pre-overlay hook 从“暗幕 → 世界粒子”调整为“世界粒子 → 暗幕”，使爆炸、命中飞溅、雨丝与风线统一受雨天环境光影响，UI 仍在暗幕之后保持清晰。完整 `clang-release` 配置/构建退出码为 0；按主人要求不运行 AutoTest，由主人自行实战验收亮度与观感。
- 2026-07-21 按主人三轮实玩发现，把每 15～25 游戏秒无条件翻转风向改为独立 50/50 重抽“吹向屋后/吹向前线”；抽中当前方向会继续维持，只有真正换向才重启定向大雨与风线视觉，避免固定左右交替被预测。`reroll_typhoon_direction` 用固定二选一点数覆盖同向与换向分支；完整 `clang-release` 配置/构建退出码为 0、无警告，当前桌面可见 `smoke_typhoon` 123 条命令通过、窗口标题确认、退出码 0，屋后/前线截图目验方向与面板正常。
- 2026-07-21 修复弹坑被当作阵风挡风墙：植物逐格吹入弹坑时改为死亡，弹坑保留，后方植物可继续填入刚腾出的源格；目标已有植物仍按原规则阻挡。完整 `clang-release` 配置/构建退出码为 0、无警告；当前桌面可见 `smoke_typhoon_crater` 27 条命令全部通过、窗口标题确认、退出码 0，状态断言邻坑植物死亡、后方植物补到第 5 列、弹坑仍在 `(2,4)`、本次阵风移动 1/损失 1，截图确认爆炸云消散且 0.45 秒视觉追赶完成后的最终布局。
- 2026-07-21 按普通关 20 多波与无尽后期仍缺乏天气压力的反馈，将天气出现率与实际威胁拆为独立曲线：普通关在 40%～75% 波次进度、黑夜无尽在第 8～20 轮平滑增强，仅把僵尸小/中/大雨倍率从 1.15/1.25/1.40 推到 1.20/1.35/1.55，植物收益、天气频率与台风概率保持不变；波次/轮次变化会刷新场上既有僵尸，冻结仍保持 0。完整 `clang-release` 配置/构建及最终增量构建均退出码 0、无警告；当前桌面可见 `smoke_weather_pressure` 57 条与 `smoke_night_rain` 84 条命令全部通过、窗口标题确认、退出码 0，确定性覆盖无尽第 8/14/20 轮与普通 30 波关卡第 20 波（压力 86%、大雨僵尸 1.53 倍）。
- 2026-07-21 独立速度压力仍会被后期成熟阵容覆盖后，加入温和版天气导演：满压力新天气权重 0/10/25/65、一次弱天气后下轮保底大雨，小/中雨随压力缩短，植物行动倍率平滑到 1.00/0.97/0.93；台风概率 75%～95%、落空后封顶 100%，满压力普通/强/超强权重 15/45/40，预报准确率最高 90%。完整 `clang-release` 配置/构建退出码 0、无警告；当前桌面可见 `smoke_weather_director` 63 条、`smoke_weather_pressure` 57 条、`smoke_typhoon` 123 条、`smoke_weather_forecast` 58 条与 `smoke_night_rain` 84 条命令全部通过，窗口标题确认、退出码均为 0。弱天气保底计数已加入存档且旧档默认 0；普通 AutoTest 隔离存档，读写兼容按源码契约审计。
- 2026-07-28 大雨闪电从 `ShowScreenFlash()` 全屏白闪改成 GameScene 程序化分叉闪电与局部散射光，寒冰菇白闪契约保持独立；`clang-playtest` 配置/构建零警告，当前桌面可见 `smoke_lightning_visual` 19 条与更新后的 `smoke_night_rain` 87 条全部通过，窗口标题确认、退出码均为 0，主放电/回闪及完整回归截图目验通过。
- 2026-07-23 新增提前 5 秒的大雨分级文字警报与多提示队列；四档各 3 句随机文案（两句七言押韵对句、一句英文），待生效台风完整初态和文案编号随档保存。`smoke_heavy_rain_prompt.json` 覆盖图片/文字并存、后出现文字处于上层、四档文案色阶、不同文案编号及超强台风切档消费；`clang-playtest` 构建通过，当前桌面可见运行 57 条命令通过，既有 `smoke_weather_forecast` 58 条回归通过，窗口均确认、退出码 0；四张专项截图已目验。
- 2026-08-23 按玩家实玩反馈修复警报有无仍可反推预报真假的对称泄露：弹窗现只跟公开预报走，预报大雨即显示、预报非大雨即隐藏；假大雨用真实大雨同一分布锁定等级和文案并可跨快照恢复，但揭晓时清除且不影响台风、天气或保底；隐藏的真实大雨仍预抽并在切档消费原待生效台风状态。同日天气预报行接入同一公开等级，大雨时显示台风/强台风/超强台风，假大雨显示诱饵等级而非大雨预报隐藏真实等级。`clang-release` 配置与全量/LTO 构建 exit 0，Win7 378 项导入审计通过；主人当前桌面可见 `smoke_heavy_rain_prompt.json` 103 条命令 exit 0、`script finished OK`，状态与同步截图确认真假大雨双向隔离、无台风不追加、三档台风文案均正确且未越界；未新增存档字段或 schema。
- 2026-08-23 天气揭晓失败判断由单一雨势扩展为公开/实际“雨势 + 台风等级”复合结果；尤其 `HEAVY → HEAVY` 续期的实际等级继续取当前 `Board::mTyphoonStrength`，不会错误消费新预抽等级。失败 UI、AutoTest 状态和关卡快照同步保存两侧等级，旧档缺字段按 `NONE` 兼容且不提升 schema。`clang-release` 全量/LTO 构建 exit 0，Win7 378 项导入审计通过；当前桌面可见 `smoke_weather_forecast.json` 最终 80 条命令、45 条断言 exit 0，`smoke_heavy_rain_prompt.json` 103 条命令、64 条断言 exit 0，两份日志均为 `script finished OK`。首轮专项只暴露脚本仍期望旧面板收回值，按当前 GAME 阶段常驻天气板契约同步后复跑通过；截图确认“预报：大雨·强台风 → 实际：大雨·超强台风”完整显示且未越界。
- 2026-07-28 高坚果接入植物阵风锚定：`Board::TriggerTyphoonPlantMove` 继续按前缘到后缘逐格结算，通过植物虚接口识别锚定格，不写死 `PLANT_TALLNUT`。高坚果自身双向免位移，只对相邻来袭格每挡一格承受800环境伤害；链条不传压、睡莲组合不按层翻倍、伤害逐格而同阵风音画按高坚果合并，第一步死亡后第二步可进入腾出格。新增 `lastGustBlockedPlantSteps` 观测值和图鉴正文；`clang-playtest` 与 `clang-release` 均零警告，可见 `smoke_tallnut_typhoon_anchor` 123条、`smoke_typhoon` 123条、`smoke_tallnut` 83条均通过、退出码0，7张专项截图目验正常。既有 `smoke_typhoon` 同步当前强台风阵风上限，把超强衰减后的剩余额度断言由2改为1。
- 2026-07-28 修复水中海豚被高坚果拦下后遭阵风吹离仍永久啃食旧目标：跳跃状态机在碰撞箱外 5px 手动 `StartEat`，原先没有碰撞对可触发退出。僵尸基类现逐帧校验目标生命、顶层身份与最多 6px 咬合间隙；吹离、目标死亡和啃食者进入死亡轨道都会同步清目标 ID 与 `eaterCount`。`clang-playtest` 与 `clang-release` 均零警告；可见 `smoke_typhoon_eating_target` 71 条通过，并回归普通/精英海豚、坚果啃食、快速读报、撑杆、水草与粉色橄榄球状态机。

详细设计见 `docs/superpowers/specs/2026-07-20-night-rain-weather-design.md`。

## 2026-08-25 整栏天气预报干扰

气象干扰僵尸接入后，Board 持有 30 游戏秒整栏黑障计时，并用雨雪/待生效台风警报、寒潮和雾势
各自的 disrupted 状态保留被截获代际。完全进场即主动施法，不等玩家先看到预报；窗口期间新广播
也会被当帧截获。栏内只显示“气象信号受干扰”，当前雨雪、台风、寒潮实况、雾势和其他天气细节
都隐藏，冬日花园独立温度计保留；锁定真实结果、pending 台风和计时不变。已显示的大雨/暴雪中央
预警、当前天气牌与失败卡在提交时撤下，窗口内同类提示被抑制，不影响其他提示。窗口与 disrupted
代际共同进入关卡 schema v5 的兼容快照；同类每次成功提交向当前余时追加 30 秒，当前同时余时
上限为 300 秒，不改变每波或整局累计口径。完整实现与可见证据见
`project_pvz_weather_jammer_zombie.md`。

## 2026-07-21 当前源码复核与精英舞王接入

本文件前文带日期的平衡值记录了较早迭代；当前源码优先。此次复核 `Board.cpp` 的当前台风参数为：前期/满压力基础概率 75%/95%，每次落空 +20 个百分点、最高 100%，普通/强/超强权重从 40/45/45 平滑到 15/45/40；僵尸顺/逆风倍率为 1.10/0.90、1.20/0.80、1.30/0.70，强/超强阵风僵尸峰值 60/90 像素每游戏秒。后续改平衡必须重新以源码核对，不能从前文旧快照抄值。

精英舞王是首个使用正式天气变异解析器的僵尸：当前在黑夜、大雨且任意台风中把波次选中的普通舞王按 50% 变异，同一波至多成功两次并持久化计数；超额成功变异候选直接跳过，不回退普通舞王，未命中变异的普通舞王仍正常生成。正式进入新波时清零，天气或台风切换不重置，类型创建后也不随天气回退；能力倍率由僵尸虚钩子派生 Board 状态。2026-07-27 起，正式实际类型创建成功后还会把永久遭遇标记写入 PlayerInfo 供图鉴解锁；解析命中、通用直造、预览和读档不会记录。详细契约见 [project_pvz_elite_dancer_zombie](project_pvz_elite_dancer_zombie.md)。

## 2026-07-24 缠绕水草接入

缠绕水草的 99→51→21→0cs 抓取倒计时属于正向植物行动，复用
`Plant::GetWeatherActionDeltaTime()`；雨势只推进这一条能力计时，不改变全局 `DeltaTime`，
也不重复加速纯视觉 `anim_grab`。51cs 后僵尸的下沉位移继续按真实游戏时间保持
100px/s，目标冻结不会阻断水草结算。可见 `smoke_tanglekelp` 在大雨 120% 植物行动倍率下
验证冻结目标仍能跨过拖沉节点，专项 74 条命令全部通过。

## 2026-07-26 鎏金冰道速度放大

黄色冰道不改 Board 的天气源，而由 `Zombie` 在统一速度合成点把能力、寒冰、雨势和台风
等每个非中性倍率分别直接强化：单层加速乘二、减速除二，因此大雨 `1.4→2.8`、
寒冰 `0.6→0.3`，中性 `1.0` 不变。活跃鎏金冰车是独立来源，重叠覆盖时逐层变换；两车处于彼此有效范围内
会互相获得两层，普通僵尸站在两条活跃黄冰道重叠区也同样获得两层。普通冰道只负责显示和
禁种，不提供速度层。冻结继续最终优先为 0，鎏金冰车自身蓄势最终倍率封顶 ×8。
更新后的可见 `smoke_gilded_zamboni` 精确通过单层大雨 280%、寒冰晴天黄冰内/外
30%/60%、双层大雨 560% 断言，证明天气源未改且逐因子新变换已接入统一动画速度层。

## 2026-07-27 僵尸自身倍率统一

`Zombie::mExtraSpeed` 已删除；僵尸自身整体倍率只由 `GetAbilityAnimSpeedMultiplier()` 提供，
再由 `UpdateAnimSpeed()` 与寒冰、雨势和黄色冰道组合。固定品种值、读报阶段值和快速铁桶实例随机值
分别从类型、已保存状态和派生类存档恢复，天气侧无需保存任何重复状态。当前桌面可见
`smoke_night_rain` 与 `smoke_gilded_zamboni` 均通过，确认雨势/减速/冻结及黄色冰道逐因子强化未改变。

## 2026-07-29 Board 展示边界

天气强度、预报锁定、台风状态和导演保底仍由 `Board` 唯一持有；天气枚举移到
`WeatherTypes.h`，而 `Board` 只通过非拥有的 `BoardPresentation` 请求警报、揭晓提示、
失败提示和程序化闪电。`GameInfoSaver` 经同一端口保存/恢复展板与失败提示的剩余时间，
这些字段仍只是 UI 瞬态，不得反向参与天气抽取或创建第二份天气状态。寒冰菇闪屏也走展示端口，
但继续与大雨局部闪电保持两套独立视觉契约。路灯花挡位菜单需要覆盖天气展板，因此由
GameScene 独占的 CardSlotManager 提供内容，并在 `GameScene::BuildDrawCommands` 中以更晚的独立 UI
命令绘制；天气展板本身的层级与状态所有权不变。当前桌面可见
`smoke_weather_forecast` 58 条和含天气状态的 `smoke_crater_card_select` 33 条均通过，
退出码为 0，错误预报、当前天气展板和轮间恢复截图已检查。

## 2026-07-29 路灯花接入与后期雾休整

4-2 起，逐格平滑雾 alpha 不再只服务绘制：路灯花照明先在 `UpdateFogCellAlpha()` 的目标值中
削减雾，再由远程植物统一通过 `CanPlantAcquireZombie()` 消费同一最终 alpha；因此视觉清晰度
与索敌许可不会出现两套范围。路灯花挡位、燃料和产光加速的完整契约见
[project_pvz_plantern_fog_core](project_pvz_plantern_fog_core.md)。
索敌允许从当前可见边界额外看入一格薄雾：目标格自身超过阈值时，只有它朝植物方向的相邻格
已经可见才放行，因此固定雾线和路灯花照明边缘共用规则，第二格浓雾仍会阻断。
雾火供给另按当前关首波到最终波的归一化进度独立收紧，不复用天气导演压力：这样未来
4-3～4-9 无论各自是 10、20 或 30 波都能走完整曲线，也不会因后期天气更强反而增加燃料。

默认雾休整仍以 50～80 游戏秒为前期边界，但随现有天气导演压力平滑缩短，在满压力时为
20～35 秒；小雾、普通迷雾和大雾仍为 35～55 秒。此调整只改变独立雾势循环，不反向修改雨势、
台风或预报权重。当前桌面可见 `smoke_plantern_fog_core` 与原有 `smoke_fog_weather` 均 exit 0。

玩家反馈满雾仍能看清底下僵尸后，绘制新增一层错位冷灰原生雾片补透明洞；它不改变
`GetFogLayerCount()`，不铺固定白色矩形，并继续乘同一个逐格 alpha。因此无照明区域明显加厚，
路灯花照亮和台风驱散仍与原有雾层同步。

## 2026-07-30 重进存档首帧迷雾同步

逐格雾 alpha 继续不入存档，由雾势、驱散量和路灯花照明派生；但读档不能把缓存清零后再从
透明平滑生成，否则退出重进会短暂暴露整张草坪。`RestoreFogState()` 仍只恢复 Board 权威状态，
待植物等实体全部还原、`StartGame()` 完成后，`Board::CompleteLoadRestore()` 会直接把每格 alpha
同步到当前目标。正常新局与游戏中雾势/照明变化仍使用原有填充和消散动画。

`smoke_fog_weather` 在 `timeScale=0` 的隔离快照重载后不等待任何帧，立即断言大雾 42% 驱散时
56 个可见雾格、最高 alpha 148、边缘/内部列 alpha 131/148，并截取同步截图。未修复版本在同一
位置确定性得到 `visibleCells=0`；修复后 `clang-playtest` 构建通过，可见专项 152 条及
`smoke_plantern_fog_core` 185 条均退出 0、`script finished OK`。

## 2026-07-30 三叶草主动改向

`Board::RedirectTyphoonFromBlover` 是外部能力改风的单一入口：有台风时同时改写持续
`mWindDirection` 与仍在进行的 `mActiveGustDirection`，重新取得 15～25 秒完整方向阶段并
重启方向相关雨/风视觉。它不改变台风强度、阵风预算/剩余时长、已完成的植物换格或迷雾驱散；
没有台风时 no-op。三叶草自身不写雾势，普通雾的既有驱散量仍会按原规则回流。

## 2026-08-02 南瓜层整组搬运

`Cell` 新增独立 pumpkin 层后，`Board::TriggerTyphoonPlantMove` 在源格校验、锚定选择、普通搬运、
出界死亡和弹坑死亡中都同时处理 `under/normal/pumpkin`。三层组合只占一个目标格，但
`lastGustMovedPlants` 按实际移动实体计数；南瓜专项以睡莲、豌豆、南瓜三层组合锁定一次强台风
向房屋移动一格后计数为 3，保存/重载前后层级不变。

## 2026-08-04 咖啡豆 overlay 层整组搬运

`Cell` 新增短时 `overlay` 后，植物阵风的源格清理、目标占用、普通搬运、出界与弹坑死亡均继续
遍历全部活跃层；不能把“全部层”退化为旧的 `under/normal/pumpkin` 固定三项。咖啡豆专项在
支持天气的日间泳池陆地格触发强台风，断言沉睡小喷菇与等待中的咖啡豆从第 3 列一起移动到第 4 列，
源格 normal/overlay 同时清空，目标格两层和咖啡豆 WAITING 状态保持不变；可见截图与脚本退出码均通过。

## 2026-08-04 扶梯格附件随台风搬运

已放置扶梯是 Board 所有、附着于植物格的共享对象，不能留在 Cell 植物层之外独立静止。现在
`Board::TriggerTyphoonPlantMove` 在成功逐格搬运植物组合时同步更新扶梯 `row/column`，并让扶梯
绘制和磁吸起点直接消费目标格宿主植物的二维阵风视觉偏移；阻挡仍留源格，出界/弹坑继续经植物
死亡链移除。屋顶专项在 0.45 秒追赶中段锁定扶梯偏移非零且与宿主误差为零，结束后两轴归零，
完整扶梯与台风回归同时通过。

## 2026-08-02 大雨闪电雷声

程序化闪电的唯一触发点 `Board::TriggerLightning()` 现在同步请求播放一次
`SOUND_THUNDER`（`resources/sounds/thunder.ogg`，请求音量 0.75），自然大雨计时与
`trigger_lightning` AutoTest 因此共用同一音画路径。`dump_state.weather` 暴露
`thunderSoundLoaded` 与 `thunderSoundPlayRequests`，专项同时锁定资源注册成功和单次触发次数；
雷声是一次性展示效果，不新增 Board 或存档状态。

## 2026-08-03 4-9 暴风雨夜

内部关卡 36 的第 22 波固定显示紫红色 `天气预报：暴风雨`；第 23～30 波立即锁定大雨、
大雾和强台风，不再参与自然天气/雾势/台风衰减。强台风沿用原有一次阵风预算，初始化标志、
闪光 pattern/余时与既有台风状态随关卡存档，读档和逐帧强制不得返还已消费阵风。暴风雨阶段
`CalculateWaveZombiePoints()` 乘 2，普通出波倒计时上限由 25 秒改为 5 游戏秒；波内血量阈值
仍可提前开波，最终波前原有 7.5 秒大波警告不变。

世界表现复刻 C# 4-10 三种黑幕/白闪节奏：常态战场全黑，闪光随 `SOUND_THUNDER` 短暂照亮，
且不叠加普通大雨的局部程序化闪电。天气覆盖层只压世界；`Scene` 中 `isUI=true` 的卡槽底板
现由 pre-overlay 接缝在 `DrawWorldOverlay()` 后、UI GameObject 前绘制，卡槽、卡片、按钮、
天气板和波次条均保持可见，未采用坐标挖洞。

`clang-release` 完整/增量构建均退出 0。当前桌面可见 `smoke_stormy_night_4_9` 71 条命令
退出 0：覆盖 22→23 波边界、4166→8666 点数、5 秒自然进入第 24 波、固定三天气、155% 僵尸
雨势倍率、93% 植物行动倍率、全黑/闪光、雷声、局部闪电停用、自然阵风从 1 消耗到 0 及
消耗后快照往返不返还阵风；三张截图
目验预报色、纯黑战场、全屏闪光与完整 UI。同步后的 4-7～4-9 出怪表回归亦可见退出 0。
普通天气回归中，`smoke_night_rain` 的暗幕断言同步当前源码 45/70/120 后 87 条可见退出 0，
`smoke_lightning_visual` 19 条亦可见退出 0，确认普通大雨仍使用局部程序化闪电。

## 2026-08-03 昼夜屋顶雨景背景

白天屋顶原图的高饱和蓝天在大雨中仍有晴空感，夜间屋顶的星空与月牙在大雨中同样不协调。
`GameScene::DrawRoofRainBackground` 现于 `LAYER_BACKGROUND + 1` 按当前昼夜背景交叉淡入
`IMAGE_BACKGROUND_ROOF_RAIN` 或 `IMAGE_BACKGROUND_NIGHTROOF_RAIN`；它位于原背景之后、
冰道与战场实体之前，植物、僵尸和 UI 仍只接受原有世界雨幕。背景 alpha 直接消费
`GetRainOverlayAlpha()`，因此沿用现有两游戏秒过渡且不新增玩法或存档状态；昼夜屋顶当前晴/小/中/大雨整数 alpha 均为 `0/96/149/255`。

昼夜雨云素材由生成式图像提供，但运行资产并非重画整张屋顶：它们分别以原始 1880×720
`background_roof.jpg` 和 `background_nightroof.jpg` 为底图，只在同一天空蒙版内换入白天灰云或深蓝紫夜云。
像素对比确认两张图的蒙版外各有 975361 个像素全部不变，屋顶下方 `y >= 367` 变化均为 0；
因此瓦缝、烟囱、树木、房屋和卫星锅保持原位。晴夜 alpha 为 0 保留星月，大雨夜 alpha 为 255 时完整换成无星月的夜云。
AutoTest 同时断言 `roofResources.rainBackgroundLoaded` 与 `roofResources.nightRainBackgroundLoaded` 的资源注册闭环。

`clang-release` 配置/链接退出 0。当前桌面可见 `smoke_roof_rain_sky` 35 条命令与
`smoke_roof_terrain_consumers` 93 条命令均退出 0，窗口标题为“植物大战僵尸中文版”，`run.log`
均为 `script finished OK`。昼夜截图已目验：白天大雨不再显露蓝天白云，夜间大雨不再显露星星月牙；
晴夜/大雨夜空瓦片区边缘相关的唯一最佳位移为 `dx=0, dy=0`（`0.965122`），原月牙区域亮像素从 1348 降为 0。
完整回归也确认清洁车、植物、弹坑、冰道、台风与昼夜屋顶均未受影响。

## 2026-08-03 天气展板透明度

按玩家反馈降低 `GameScene::DrawWeatherPanel` 深蓝底板的遮挡：主底色 alpha 从 218 降为 176，
会与其重叠的错位阴影 alpha 从 92 降为 64；文字、边框与左侧天气强度色条不变。此改动只影响
绘制，不新增 Board、展示计时或存档状态。

`clang-release` 配置与构建退出 0。当前桌面可见最小天气板专项 9 条命令退出 0，窗口标题为
“植物大战僵尸中文版”，`run.log` 为 `script finished OK`；截图目验确认战场纹理能透过底板，
两行文字、边框和状态色条仍清晰。

## 2026-08-09 屋脊督军直接改天

实体主动改天气仍不能持有第二份雨势状态。`RoofMarshalZombie` 只按自身已保存的指挥次数与生命
跨段发出命令，`Board::TriggerRoofMarshalWeather` 才负责检查天气支持、暴风雨夜锁定、强度优先级与
同档续期策略，并复用 `BeginRain` 的两秒过渡、粒子、声音、天气板和既有存档字段。当前入口只接受
中雨/大雨且只升不降：周期中雨不续同档，避免固定技能永久锁雨；残血大雨只在跨越 4000 时调用一次，
允许把已有大雨补足到 30 秒。技能本身不额外抽取台风，自然大雨已存在时同档补时保留其既有台风。

督军每第 3 次实际指挥在晴/小雨时强制 20 秒中雨，首次进入 `1..3999` 生命时强制至少 30 秒大雨；
不支持天气的第一大关 no-op，已有大雨不会被周期命令降级或刷新。`smoke_roof_marshal_weather` 可见
exit 0，覆盖上述边界、两秒过渡、无额外台风、后续受击不重置和两次隔离快照往返；
`smoke_night_rain`、`smoke_roof_rain_sky` 同步可见 exit 0。

本次还确认三份旧天气脚本存在 HEAD 既有断言债务，并非督军入口引入：源码当前
`kAdventurePressureFullProgress=0.7`、`kLateWeatherForecastAccuracyPercent=95`，而
`smoke_weather_pressure`/`smoke_weather_director` 仍按旧 75% 曲线结果 86% 和 90% 上限断言；
`smoke_weather_forecast` 的末尾面板也会被随后自然生成的新预报重新拉回。未在首领任务中擅自改变
这些平衡常量或旧脚本，后续专门整理天气导演时必须先让主人确认 70%/95% 是否为当前定案。

## 2026-08-13 开局台风保护与控制台开关

为减少玩家因早期强/超强台风反复刷开局，`PlayerInfo` 新增默认开启的
`openingTyphoonProtectionEnabled`，主菜单“控制台”可随时关闭。开启时，冒险关和生存首轮的
第 1～5 波仍允许正常晴雨循环，但新大雨附加台风概率固定为 0；进入第 6 波恢复现有天气导演概率，
生存第二轮起不重复保护。关闭开关后从开局即完整使用原规则，满足主动追求高难度的玩家。

保护期不是一次随机落空：直接大雨路径不增加 `heavyPhasesWithoutTyphoon`；预警期锁定的
“保护性无台风”另以 `pendingHeavyTyphoonOpeningProtected` 随关卡档保存，揭晓后同样不累计 pity，
避免第 6 波被保护期反向推成近似必出台风。旧玩家档缺开关字段时回到默认开启，旧关卡档缺 pending
标志时按普通随机落空解释，两者都可由中性默认兼容，无需提升 schema。

`clang-release` 完整配置/构建退出 0、零警告，Win7 x64 导入审计通过 378 项；`ctest` 的
`save-migration` 与 `save-schema` 均通过。当前桌面可见 `smoke_opening_typhoon_protection` 59 条、
更新后的 `smoke_mainmenu_console` 30 条和 `smoke_typhoon` 124 条命令均退出 0，窗口标题均确认，
三份 `run.log` 均以 `script finished OK` 结束。控制台四张截图确认新增第三项默认勾选、关闭后重开
保持未勾选且布局无重叠；台风父回归的强/超强阵风截图目验正常。`adding-rain-weather` 的核心契约、
reference 与项目指南命令表已同步，并经 skill-creator `quick_validate.py` 校验通过。
