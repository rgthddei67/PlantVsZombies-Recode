---
name: project_pvz_elite_scaredyshroom
description: 2026-07-23 精英胆小菇：2-8奖励，连续吐孢子成长攻速和伤害，受惊归零，白天低速成长
metadata:
  node_type: memory
  type: project
---

# 精英胆小菇

## 当前契约

- 类型 `PLANT_ELITE_SCAREDYSHROOM` 追加在旧植物枚举末尾，冒险显示关卡 2-8（内部 level 17）通关奖励；卡片 225 阳光、20 秒冷却。
- `EliteScaredyShroom : ScaredyShroom` 复用既有第 25 帧吐弹事件与四态害怕状态机。生命 500；初始孢子伤害 10，每 10 发 +1，最高 15；初始间隔 1.5 秒，每 5 发间隔缩短 15%，最低 0.2 秒。成长进度最多 65。
- 受惊开始时成长进度清零，因此攻速、伤害都回到初始值。白天不睡觉，但每次成功吐弹只增加 0.6 成长，较夜间慢 40%；小数进度参与存档。
- 每个关卡累计最多种植 4 株，死亡、铲除或被台风吹走都不返还次数；模仿者落种即按目标预占，变身不重复计数。累计数由 `Board` 保存；旧档无字段或已有字段漏计时，都取累计字段与档内本体/待变身模仿者数量的较大值（封顶 4）。
- 游戏内复用胆小菇 reanim 并施加紫色 overlay，卡片使用 `scripts/recolor_elite_scaredyshroom.ps1` 从原卡图可重复生成独立紫色资源。

## 高速射击约束

- 射击间隔不能直接等同于整段动画重播速度。`mShotPending` 从起播保持到第 25 帧吐弹事件，事件真正创建孢子后才允许下一次重播；否则 0.2 秒档会在到达帧事件前反复重启动画，表现为“最后几下很快但没有子弹”。
- 高速动画只需把 16→25 帧约 0.75 秒的吐弹前摇压进当前间隔，速度为 `attackSpeedMultiplier * max(1.5, 0.75 / interval)`，不要按完整 1.5 秒基准成倍提速。
- 害怕守卫只覆盖吐弹前的 pending 阶段；不能屏蔽整段 `anim_shooting`，否则 0.2 秒连续重播会让植物永久无法受惊。
- 目标缓存刷新为 0.1 秒并与射击计时独立，保证最低 0.2 秒间隔不会额外被索敌节流拖慢。

## 验证

- 桌面可见 `smoke_elite_scaredyshroom.json` 覆盖 500 生命、65 成长、13 攻速阶段、15 伤、200ms、同屏多发孢子、受惊全清、白天单发增长 0.6，以及与普通胆小菇/小喷菇的视觉站位。本次三株限制按主人要求未运行 AutoTest，仅做编译验证。
- `smoke_adventure_progression.json` 覆盖 2-8 奖杯领取后 `adventureLevel=18` 且精英胆小菇卡片归属成立。
- 普通玩家存档仍隔离；`save_level_snapshot/reload_level_snapshot` 可走正式序列化与新场景重载。

## 2026-09-05 累计上限修复

- 旧程序可复现：生存第 2 轮选卡快照重载后，开战未结束 `mIsLoadSave` 生命周期，玩家后续落种持续豁免累计次数。直接 GAME 快照恢复已有清理，不能泛化为所有读档都会触发，也不能仅据玩家截图认定触发来源。
- 玩家创建提交不再受读档标记豁免；无 ID 的历史模仿者恢复显式走内部非玩家入口。选卡过场结束补齐 `CompleteLoadRestore()`，保持原有小推车初始化顺序。
- `smoke_elite_scaredyshroom_quota` 在旧 EXE 上于“4 株后仍可种”失败；修复后覆盖轮间恢复、新局、普通/模仿者共用次数、占格失败不扣次、铲除/死亡不返还和快照往返。
- 当前 `clang-release` 可见专项通过，退出码、状态、日志与截图已核对；`smoke_elite_scaredyshroom_quota_new_game` 不执行任何存读档，验证普通 4 株及第 5 株真实点击拒绝。隔离派生快照另实测累计字段为 0、缺累计字段、植物缺 ID 三种历史格式，均恢复为 4 次且拒绝第 5 株；这不证明截图玩家的具体触发路径。
- 技能/资源审计：补充累计名额与读档生命周期契约并通过 `quick_validate.py`；本次无卡图、reanim、子弹、粒子或占位绘制变更。
- 前述成长参数与旧专项记录是历史值；本次只修累计限制，不调整当前植物战斗数值。
