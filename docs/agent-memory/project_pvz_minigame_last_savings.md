# 小游戏：最后的家底

## 2026-09-05 玩法与入口

- 主菜单现有小游戏石碑通过延迟切场景进入 `GameSelectScene` 的 `MINIGAMES` 页；第一关直接开放，不依赖冒险解锁。
- `Game/MiniGameDefinition.h` 集中定义内部关卡号 2000、七张卡、3000 初始阳光、10 波、首波准备 60 游戏秒和后续最长波间隔 35 游戏秒。
- 复用白天五路草坪。`Board` 持有全部玩法状态，关卡身份从 `mLevel` 派生；限制同时覆盖 `CanPlantAt` 与正式植物创建入口。天空不落阳光，收益入口 `AddSun` 不接受补给；花费沿正式卡槽流程扣除，铲除不退款。
- 七张卡为豌豆、寒冰豌豆、双发、坚果、土豆雷、樱桃炸弹和窝瓜，选卡面板自动预选。小游戏卡池不写冒险拥有记录，提交也不覆盖冒险的上次选卡；面板隐藏上次选卡按钮并展示玩法说明。
- 波次用 `Board::TrySummonZombie` 的专属编排，经 `CreateOrQueueWaveZombie` 正式入口创建，复用已有品种及其动画、伤害、死亡和存档。没有新增实体品种、动画帧事件或资源副本。

## 波次编排

| 波次 | 每路纵深 | 加入的压力 |
|---|---|---|
| 1-2 | 1 队 | 普通僵尸，留时间铺阵 |
| 3 | 2 队 | 路障混编 |
| 4 | 2 队 | 报纸 |
| 5 | 2 队 | 撑杆 |
| 6 | 2 队 | 铁门，考验灰烬/压杀预算 |
| 7 | 2 队 | 后排铁桶 |
| 8-9 | 3 队 | 一路橄榄球 |
| 10 | 4 队 | 第 1/3/5 路橄榄球与前述混编 |

表内为每波编排，不是同时存在或整局累计上限。纵深间距为当前 Cell 宽度；清场可触发提前下一波，最长间隔仍为 35 游戏秒。普通关卡的难度、植物伤害和品种能力继续使用现有配置。

## 存档、结算与 UI

- 使用既有独立 `level2000_data.json`，预算、波次、倒计时、卡牌冷却与实体沿正式 schema 保存；没有新增需要迁移的持久化字段，读档不会再次发放初始预算。
- 失败、重开、清场奖杯均沿现有生命周期。小游戏结束回到小游戏选择页，奖杯不会推进冒险进度或发冒险奖励。
- 长关卡名称使用右边界 865px 的右对齐布局，避开从 870px 开始的波次进度条；开局准备倒计时与无补给说明在战场顶部显示。

## 验证入口

`autotest/scripts/smoke_minigame_last_savings.json` 先通过真实主菜单、选关、开始和一次卡槽/草坪点击验证扣费，再用总成本 2875 阳光的固定阵型夹具与 125 余款验证快照和自然十波。固定阵型夹具不验证完整布阵的卡牌冷却操作。`smoke_minigame_navigation.json` 补充零预算、失败重开、退出选关与冒险上次选卡隔离。

## 本次交付证据与限制

- 主人实际操作试玩后反馈“挺不错”，并要求不再继续重跑；随后停止 AutoTest 验收。
- 最终 `clang-release` 构建成功，Win7 导入审计 378 项通过；本次 CTest 3/3 通过。
- 可见试运行确认了小游戏入口、独立七卡、3000 初始阳光、禁止向日葵、正常种植扣费、预算/阵型快照往返及加强后的敌人。曾在主人同时操作的一局生成第十波奖杯，该结果不能作为无人干预的自动通关证据。
- 长串真实输入用例因手动操作混入及偶发漏点未取得完整 passed。最后将脚本拆为少量真实输入与预算内固定阵型自然战斗；修订后的完整脚本及导航专项按主人指示未再运行，不宣称自动验收全部通过。
- 最后一次选卡说明坐标修正已构建，尚未重截最终选卡页。右下角长名称避让进度条已有前一轮截图实证。
- 已审计 adding-plant、adding-zombie 及相关出怪节奏参考：本次复用既有实体，没有改变其接口、资源注册、所有权、动画事件或实体存档契约；现有技能无须更新。地图和运行资产未改动。
