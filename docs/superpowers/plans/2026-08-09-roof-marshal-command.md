# 5-9 屋脊督军指挥技能实施计划

1. 扩展 `RoofMarshalZombie`：增加指挥阶段、1/9/7 秒计时、1.2 秒停步姿势、固定原版白名单、避开首领行的唯一随机行召唤和 4000 以下四只逻辑。
2. 增加高血量定点指挥与 6 秒相邻换行、独立平滑视觉补偿；4000 以下原子停止换行并恢复普通水平推进。
3. 覆写本体伤害阈值，完全取消督军的残血掉头临界值，仅在生命归零时播放专属掉头并进入死亡流程。
4. 完成 `SaveExtraData`/`LoadExtraData`，保存全部影响后续召唤与换行的状态并修复非法旧值。
5. 将 `BossSlot::RESERVED` 改为 `ROOF_MARSHAL`，在 `Board::SummonNextWave()` 的 5-9 第 15 波额外生成一只中路、可见纵深督军。
6. 扩展 AutoTest 状态投影与脚本，验证正式波次、固定白名单、召唤时序、不同随机行、定点换行、最终阶段走路和读档。
7. 由 Board 增加只升不降的督军改天入口：每三次指挥尝试 20 秒中雨，首次进入 4000 以下补足 30 秒大雨；不额外抽取台风，也不让周期技能续期同档天气。
8. 使用 `clang-release` 构建，从 `build/clang-release` 在主人当前桌面可见运行最小相关 AutoTest；检查退出码、`run.log`、状态 JSON、断言与截图。
9. 审计并更新僵尸、雨天技能与仓库记忆，运行 skill-creator `quick_validate.py`，再提交已验证改动；推送另行取得主人对本次新提交的明确同意。
