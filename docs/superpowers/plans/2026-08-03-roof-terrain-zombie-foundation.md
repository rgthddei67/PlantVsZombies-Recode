# 屋顶地形与僵尸路径基础实施计划

日期：2026-08-03

1. 在 `ResourceKeys.h` 与 `GameScene.cpp` 接通白天/黑夜屋顶背景。
2. 在 `Board` 集中实现屋顶判断、85px 五行离散格位与按 X 连续坡面高度。
3. 让 Cell、植物生成、僵尸生成/读档、开发者选行全部消费新的几何入口。
4. 在 `Zombie` 基类集中同步屋顶 Y，并修正矿工/伴舞的当前 X 裁剪底边。
5. 在 `CanPlantAt` 写明并锁定屋顶临时直种 normal 层规则。
6. 扩展 AutoTest 状态：棋盘格高/格中心、屋顶坡顶、僵尸相对地形高度。
7. 新增 `smoke_roof_zombie_foundation.json`，覆盖背景、格位、直种、普通/气球/矿工和快照。
8. 构建 `clang-release`，在主人当前桌面可见运行专项，核对退出码、日志、状态、断言和截图。
9. 审计相关技能与项目记忆，记录可复用屋顶地形契约，校验后提交并按仓库状态决定 push。
